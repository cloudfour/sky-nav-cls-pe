---
/******
 * Don't use this component!!!
 * It doesn't work well for some reason
 * Possibly related: https://github.com/withastro/astro/issues/2769
 */

// Use Cloud Four pattern styles
import '../../node_modules/@cloudfour/patterns/dist/standalone.css';

export interface Props {
	title: string;
}

const { title } = Astro.props as Props;
---

<head>
	<title>{title}</title>
	<meta charset="UTF-8" />
	<link rel="icon" type="image/svg+xml" href="/cloud-four.svg" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>
